<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <a class="text-blue-500" href="/">Back To Home</a>
        <h1 class="text-3xl tracking-tighter mb-3 mt-10">Edit User</h1>
        <% console.log(users) %>
        <form action="/update/<%= users._id %>" method="post">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-transparent outline-none" type="text" placeholder="Name" name="name" value="<%= users.name %>">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-transparent outline-none" type="text" placeholder="Email" name="email" value="<%= users.email %>">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-transparent outline-none" type="text" placeholder="Image Url" name="image" value="<%= users.image %>">
            <input class="px-5 py-2 bg-blue-500 rounded-lg ml-3" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>