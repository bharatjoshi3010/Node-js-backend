<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class = "w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5 " href="/logout">Logout</a>
        </div>
        
        <h3 class="text-3xl mb-5">Profile </h3>
        <h3 class="text-2xl ">Hello, <%= user.name %> 👋</h3>
        <!-- we get the user's details from the profile route and now we can use it here -->

        <h5 class="mb-5">You can create a new post.</h5>
        <form method="post" action="/post">
            <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md outline-none block " placeholder="Enter your thoughts here " name="content"></textarea>
            <input class="block w-40 px-3 py-2 bg-blue-500 rounded-md mt-2" type="submit" value="Create new post">
        </form>
        <div class = "posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.post.reverse().forEach(function(post){%>     
                    <!-- this reverse helps us to show the recent post in top -->
                <div class="post w-1/3 p-4 mb-4 border-[1px] border-zinc-700 bg-zinc-800 rounded-md">
                    <h4 class="text-blue-500 mb-2 "><%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-4 inline-block"><%= post.likes.length %> likes</small>
                    <div class="btns flex mt-1 gap-4">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            <!-- if user already liked then we show Unlike otherwise we show like -->
                        </a>
                        <a class="text-zinc-600" href="">Edit</a>
                    </div>
                </div>
                <%}) %>
            </div>
        </div>
    </div>
</body>
</html>