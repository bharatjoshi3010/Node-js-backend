<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between px-5 py-3">
        <h3 class="text-xl">Watches</h3>
        <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
        <% if(loggedin){ %>
            <div>
            <a href="/shop">Shop &nbsp &nbsp &nbsp</a>
            <a href="/cart">Cart &nbsp &nbsp &nbsp</a>
            <a class="text-red-600" href="/users/logout">Logout</a>

        </div>
        <% } else {%>
            <div>
            <a class="text-red-200" href="/owners/">if owner, Add product</a>

        </div>
            <%}%>
        
    </nav>