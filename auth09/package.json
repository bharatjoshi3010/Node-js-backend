{
  "name": "auth09",
  "version": "1.0.0",
  "description": "authentication : verifying the user before giving him access\r authrization :  checking the access permits(like -> is user can change the database or just a read-only user )\r in this tutorial we see \r 1-> how to set cookies\r 2-> use of  bcrypt for password encryption and decryption\r 3-> Jwt kya hai and How to store and retrive data from Jwt",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  }
}
